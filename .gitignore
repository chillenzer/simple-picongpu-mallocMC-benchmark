# SPDX-FileCopyrightText: 2024-2026 Institute of Radiation Physics, Helmholtz-Zentrum Dresden-Rossendorf
# SPDX-License-Identifier: MIT

__pycache__/
*.pyc

# generated by the Makefile
src/
build/
run-stamps/
output/
figures/
ro-crate-metadata.json
ro-crate.crate.zip

# frozen legacy benchmark data (the raw logs and the frozen table stay
# local; only the legacy/ scripts and docs are committed)
legacy/logs/
legacy/legacy_results.h5

# frozen microbenchmark data (the raw CSVs and the frozen table stay
# local; the microbenchmarks/memmansurvey submodule is pinned code)
microbenchmarks/data/
microbenchmarks/microbench_results.h5

# local patch artifacts
*.patch

# local working notes (quarantined internal notes from before the
# publish cleanup; the publish-prep history/tag notes-pre-publish holds
# their last committed versions)
/notes/

# local data artifacts (machine data kept out of the repository)
/tmp.zip
