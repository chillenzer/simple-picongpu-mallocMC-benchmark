[tool.ruff]
line-length = 120
target-version = "py314"

[tool.ruff.lint]
# Ruff's default selection (E4, E7, E9, F) plus import sorting (I); line
# wrapping is left to `ruff format`.
select = ["E4", "E7", "E9", "F", "I"]
